'''
정수 두 개를 한 줄에 입력받아 합을 출력
'''

a, b = map(int, input().split())
result = a + b
print("두 정수의 합은", result, "입니다.")

'''
위 코드에서 input() 함수를 사용하여 사용자로부터 정수 두 개를 입력받습니다.
입력받은 문자열을 split() 함수를 사용하여 공백을 기준으로 분리한 뒤,
map() 함수를 사용하여 분리된 문자열을 정수형으로 변환합니다.
map() 함수는 여러 개의 데이터를 한 번에 다른 형태로 변환해줍니다.
'''
